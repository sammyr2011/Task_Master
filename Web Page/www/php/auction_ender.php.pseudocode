<?php
//Run this once a minute to end auctions

$timestamp = getUnixTime();
$openAuctions = getOpenAuctionsFromDB();

for each $auction in $openAuctions
{
  if($auction.timestamp <= $timestamp)
  {
    $auction.end();
    notifyWinner($auction.highestBidder);
  }
}

?>
